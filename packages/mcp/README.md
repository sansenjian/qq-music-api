# QQ Music API MCP

MCP stdio server for `@sansenjian/qq-music-api`.

```bash
npm install @sansenjian/qq-music-api-mcp
qq-music-api-mcp
```

The core API package stays separate so normal API consumers do not install MCP-specific dependencies.
